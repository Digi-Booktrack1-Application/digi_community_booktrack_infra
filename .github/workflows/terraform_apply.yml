name: Terraform EKS CI/CD

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Select Environment"
        required: true
        type: choice
        options:
          - dev
          - prod

# PREVENT MULTIPLE TERRAFORM RUNS
concurrency:
  group: terraform-${{ github.event.inputs.environment }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: write

jobs:

# #########################################
# VERSIONING JOB
# #########################################

  versioning:
    runs-on: ubuntu-latest

    outputs:
      version: ${{ steps.bump.outputs.version }}

    steps:

      # CHECKOUT
      - name: Checkout Code
        uses: actions/checkout@v3

      # VERSION INCREMENT
      - name: Read & Increment Version
        id: bump
        run: |

          FILE="version.txt"

          if [ ! -f $FILE ]; then
            echo "v1.0.0" > $FILE
          fi

          VERSION=$(cat $FILE)

          echo "Current version: $VERSION"

          MAJOR=$(echo $VERSION | cut -d. -f1 | tr -d 'v')
          MINOR=$(echo $VERSION | cut -d. -f2)
          PATCH=$(echo $VERSION | cut -d. -f3)

          PATCH=$((PATCH + 1))

          NEW_VERSION="v$MAJOR.$MINOR.$PATCH"

          echo "New version: $NEW_VERSION"

          echo $NEW_VERSION > $FILE

          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT

      # COMMIT VERSION
      - name: Commit Updated Version
        run: |

          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git add version.txt

          git commit -m "Auto bump version to ${{ steps.bump.outputs.version }}" || echo "No changes"

          git push

# #########################################
# TERRAFORM DEPLOY
# #########################################

  deploy:
    needs: versioning

    runs-on: ubuntu-latest

    steps:

      # CHECKOUT
      - name: Checkout Code
        uses: actions/checkout@v3

      # SELECT ENVIRONMENT
      - name: Set Environment Variables
        id: env
        run: |

          ENVIRONMENT="${{ github.event.inputs.environment }}"

          echo "Selected environment: $ENVIRONMENT"

          if [ "$ENVIRONMENT" = "dev" ]; then

            echo "TF_PATH=pillars/dev" >> $GITHUB_OUTPUT

          elif [ "$ENVIRONMENT" = "prod" ]; then

            echo "TF_PATH=pillars/prod" >> $GITHUB_OUTPUT

          else

            echo "Invalid environment selected"
            exit 1

          fi

      # AWS LOGIN
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-1
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}

      # TERRAFORM SETUP
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      # TERRAFORM INIT
      - name: Terraform Init
        working-directory: ${{ steps.env.outputs.TF_PATH }}
        run: |
          terraform init -upgrade

      # TERRAFORM VALIDATE
      - name: Terraform Validate
        working-directory: ${{ steps.env.outputs.TF_PATH }}
        run: |
          terraform validate

      # TERRAFORM PLAN
      - name: Terraform Plan
        working-directory: ${{ steps.env.outputs.TF_PATH }}
        run: |

          VERSION="${{ needs.versioning.outputs.version }}"

          terraform plan \
            -lock-timeout=10m \
            -var="app_version=$VERSION"

      # TERRAFORM APPLY
      - name: Terraform Apply
        working-directory: ${{ steps.env.outputs.TF_PATH }}
        run: |

          VERSION="${{ needs.versioning.outputs.version }}"

          terraform apply \
            -auto-approve \
            -lock-timeout=10m \
            -var="app_version=$VERSION"
